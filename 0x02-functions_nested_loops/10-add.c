#include "main.h"
/**
 * add - gives the sum of two numbers
 * @a: the variable that stores the value of the first int
 * @b: the variable that stores the value of the second int
 * Return: i which is the sum of the two integers
 */
int add(int a, int b)
{
	int i;

	i = a + b;
	return (i);
}
