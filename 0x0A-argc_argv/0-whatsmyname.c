#include <stdio.h>
/**
 * main - entry point
 * @argc: the number of commandline argument
 * @argv: an array containing commandline arguement
 * Return: argc
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (argc);
}
